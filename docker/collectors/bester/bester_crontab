SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin
# m h dom mon dow user  command
@reboot ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d dailyuk
@daily ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d --build dailyuk
@reboot ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d eupop
@daily ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d --build eupop
@reboot ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d k_fertility
@daily ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d --build k_fertility
0 0 * * 1,4 ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d --build migrationuk_mon_all
0 0 * * 2,5 ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d --build migrationuk_tue_men
0 0 * * 3,6 ubuntu cd ~/mySocialWatcher/docker/collectors/bester; docker-compose up -d --build migrationuk_wed_women
